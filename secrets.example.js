/**
 * Copy this file to secrets.js for local OpenAI testing (API_MODE: "local" in script.js).
 * secrets.js is gitignored — do not commit real keys.
 * For production, use your Cloudflare Worker only; keep OPENAI_API_KEY empty here.
 */
window.OPENAI_API_KEY = "";
